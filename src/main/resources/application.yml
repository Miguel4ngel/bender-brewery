server:
  port: 8082

spring:
  jpa:
    database: POSTGRESQL
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    platform: postgres
    url: jdbc:postgresql://localhost:5432/bender-brewery-schema
    username: postgres
    password: admin12345
    driverClassName: org.postgresql.Driver
